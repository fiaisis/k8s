---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ir-api
  namespace: ir
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ir-api
  template:
    metadata:
      labels:
        app: ir-api
    spec:
      containers:
      - name: ir-api
        image: ghcr.io/interactivereduction/ir-api@sha256:86d82458ab3dcb4d1daafce1a6b444bc016abfd81b71fe329d3a638467dbd4ad
        env:
        - name: DB_IP
          value: "{{ db_prod.ip }}"
        - name: DB_PASSWORD
          value: "{{ db_prod.password }}"
        - name: DB_USERNAME
          value: "{{ db_prod.username }}"
        ports:
          - name: api-port
            protocol: TCP
            containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: ir-api-service
  namespace: ir
spec:
  selector:
    app: ir-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80