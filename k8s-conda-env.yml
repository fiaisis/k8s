name: k8s

channels:
  - conda-forge

dependencies:
  - ansible
  - python-kubernetes
  - minikube
  - kubernetes
  - kubernetes-helm
  - terraform
  - openstacksdk==0.61.0 # Requirement for using ansible to deploy openstack compute instances